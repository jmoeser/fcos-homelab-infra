[Unit]
# Trigger image pruning after every auto-update run.
# podman-image-prune.service is ordered After=podman-auto-update.service,
# so it runs once this service completes.
Wants=podman-image-prune.service
